[[_STL]]

（若有64位版本，优先列出，可以很容易知道去掉末尾`ll`即32位版本，`u64`代表`unsigned ll`）
`__builtin_popcountll(u64)` 计算二进制1的个数，常用
`__builtin_parityll(u64)` 计算奇偶校验，完全等效于`popcount&1`
`__builtin_clzll(u64)` 计算前导零个数(count leading zero)，可用`__lg`代替
`__builtin_ctzll(u64)` 计算尾随零个数，可用`__lg`配`x&-x`代替，但推荐也记下