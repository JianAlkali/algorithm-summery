[[STL]]

（若有64位版本，优先列出，可以很容易知道去掉末尾ll即32位版本，u64代表unsigned ll）
__builtin_popcountll(u64) 计算二进制1的个数
__builtin_parityll(u64) 计算奇偶校验，完全等效于popcount&1
__builtin_clzll(u64) 计算前导零个数(count leading zero)，可用__lg代替
__builtin_ctzll(u64) 计算尾随零个数，可用__lg配x&-x代替