无特殊说明，其他代码均默认加上这段代码。
## 新版：
调整了`all`的定义，避免了对`array`错误调用导致错误。
```cpp
#include <bits/stdc++.h>
using namespace std;
using ll=long long;
#define int ll
using i128=__int128_t;
using pii=pair<int,int>;
using vi=vector<int>;
using vvi=vector<vector<int>>;
#define all(v) ((v).begin()),((v).end())
#define endl '\n'
#define _ << ' ' <<

constexpr ll INF=3e18;
constexpr ll MOD=998244353;
constexpr ll N=5e5+9;
constexpr ll M=1e5+9;

ll qpow(ll a,ll b,ll mod=MOD){ll r=1;while(b){if(b&1) r=r*a%mod;b>>=1;a=a*a%mod;}return r;}

void solve(){
    
}

signed main(){
    ios::sync_with_stdio(false); cin.tie(nullptr);
    int T=1;
    cin>>T;
    while(T--)
        solve();
    return 0;
}
```
## 旧版：
```cpp
#include <bits/stdc++.h>
using namespace std;
using ll=long long;
#define int ll
using i128=__int128_t;
using pii=pair<int,int>;
using vi=vector<int>;
using vvi=vector<vector<int>>;
#define all(v) (begin(v)),(end(v))
#define bll(v) (begin(v)),(begin(v))
#define endl '\n'
#define _ << ' ' <<
constexpr ll INF=3e18;
constexpr ll MOD=998244353;
constexpr ll N=5e5+9;
constexpr ll M=1e5+9;
ll qpow(ll a,ll b,ll mod=MOD){ll r=1;while(b){if(b&1) r=r*a%mod;b>>=1;a=a*a%mod;}return r;}
void solve(){
}
signed main(){
    ios::sync_with_stdio(false); cin.tie(nullptr);
    int T=1;
    cin>>T;
    while(T--)
        solve();
    return 0;
}
```