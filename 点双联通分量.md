[[图论]]

是对无向图而言的，点双连通分量内删掉任意一点后图仍连通。
对于一个n个节点m条无向边的图，请输出其点双连通分量的个数，并且输出每个点双连通分量。输入第一行，两个整数n和m。接下来m行，每行两个整数 ，表示一条无向边。输出第一行一个整数x表示点双连通分量的个数。接下来的x行，每行第一个数a表示该分量结点个数，然后a个数，描述一个点双连通分量。你可以以任意顺序输出点双连通分量与点双连通分量内的结点。
```cpp
struct E{
    int v,nxt;
}edge[M<<1];
int head[N], ecnt=1;
inline void add(int u,int v){
    edge[ecnt]={v,head[u]};
    head[u]=ecnt++;
}
int vis[N],dict[N],low[N]; //跑过、dfs序、最低可达dfs序
vi ans[N]; int ansn=0;
inline void clear(int n){ // 清空全局
    fill(head,head+n+1,0);
    fill(vis,vis+n+1,false);
    fill(dict,dict+n+1,INF);
    fill(low,low+n+1,INF);
    for(int i=0;i<=n;i++){
        ans[i].clear();
    } ansn=0;
    ecnt=1;
}
inline void solve(){
    int n,m;
    cin>>n>>m;
    clear(n);
    for(int i=0,u,v;i<m;i++){
        cin>>u>>v;
        add(u,v);
        add(v,u);
}
```

```cpp
    int tt;
    stack<int> s;
    auto dfs=[&](auto&& dfs,int u,int f)->void{
        int child=0;
        dict[u]=low[u]=tt++;
        s.emplace(u);
        vis[u]=true;
        for(int eg=head[u];eg;eg=edge[eg].nxt){
            int v=edge[eg].v;
            if(v==f)
                continue;
            if(vis[v]) //vis过，low从dict更新
                low[u]=min(low[u],dict[v]);
            else{
                child++;
                dfs(dfs,v,u);
                low[u]=min(low[u],low[v]); //否则low从low更新
                if(low[v]>=dict[u]){ // 找到了
                    while(!s.empty()){
                        int tp=s.top();
                        ans[ansn].emplace_back(tp);
                        s.pop();
                        if(tp==v)
                            break;
                    }
                    ans[ansn].emplace_back(u);
                    ansn++;
                }
            }
        }
        if(f==0 && child>=1){
            while(!s.empty())
                s.pop();
        }
        return;
}; //~dfs
```

```cpp
    for(int i=1;i<=n;i++){ // 图不一定连通
        if(!vis[i]){
            tt=0;
            dfs(dfs,i,0);
            if(!s.empty()){ // 对于i出发只有一条边的特判
                while(!s.empty()){
                    ans[ansn].emplace_back(s.top());
                    s.pop();
                }
                ansn++;
            }
        }
    }
    cout<<ansn<<'\n'; //输出
    for(int i=0;i<ansn;i++){
        int an=ans[i].size();
        cout<<an<<' ';
        for(int j=0;j<an;j++)
            cout<<ans[i][j]<<" \n"[j==an-1];
    }
    return;
} //~solve
```
